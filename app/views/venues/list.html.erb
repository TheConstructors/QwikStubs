<% if @venues.blank? %>
<h1>There are no venues</h1>


<% else %>

<table class="table">
<tr>
  <th>Name</th>
  <th>City</th>
  <th>State</th>
</tr>
<% @venues.each do |v| %>
<tr>
  <td><%= v.name %> </td>
  <td><%= v.city %> </td>
  <td><%= v.state %> </td>
</tr>
<% end %>
</table>
<% end %>
